{
  "name": "sql-injection",
  "expected": [
    { "id": "sql-injection", "file": "src/users.js", "pattern": "injection|interpolat|parameteriz|escap|sanitiz" }
  ],
  "input": {
    "branch": "refactor/user-query",
    "target": "main",
    "commits": ["refactor: simplify user lookup"],
    "files": [{ "path": "src/users.js", "additions": 1, "deletions": 1 }],
    "custom_instructions": null,
    "impact_candidates": null,
    "diff": "diff --git a/src/users.js b/src/users.js\n--- a/src/users.js\n+++ b/src/users.js\n@@ -1,5 +1,5 @@\n import { db } from './db.js'\n \n export function getUser(id) {\n-  return db.query('SELECT * FROM users WHERE id = ?', [id])\n+  return db.query(`SELECT * FROM users WHERE id = ${id}`)\n }\n"
  }
}
