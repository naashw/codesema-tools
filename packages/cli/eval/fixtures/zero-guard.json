{
  "name": "zero-guard",
  "expected": [
    { "id": "empty-average", "file": "src/stats.js", "pattern": "empty|zero|NaN|length" }
  ],
  "input": {
    "branch": "feat/average",
    "target": "main",
    "commits": ["feat: add average helper"],
    "files": [{ "path": "src/stats.js", "additions": 4, "deletions": 0 }],
    "custom_instructions": null,
    "impact_candidates": null,
    "diff": "diff --git a/src/stats.js b/src/stats.js\n--- a/src/stats.js\n+++ b/src/stats.js\n@@ -1,3 +1,7 @@\n export function sum(values) {\n   return values.reduce((acc, v) => acc + v, 0)\n }\n+\n+export function average(values) {\n+  return sum(values) / values.length\n+}\n"
  }
}
